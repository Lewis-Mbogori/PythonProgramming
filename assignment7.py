# create a list of 5 numbers
# create a variable counter and assign it zero

# Iterate over the list and return the largest number

# codewars(kata)
# hackerank
# reassignment

numbers = [10, 20, 30, 16, 9]
counter = 0

for number in numbers:
    if counter < number:
        counter = number

print(counter)

# Get the smallest number
# Hint: counter should have the largest number